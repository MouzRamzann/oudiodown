# Path to an Instagram cookies file for authentication.
# Accepts either Netscape format (.txt) or a JSON array exported from
# browser extensions like EditThisCookie or Cookie-Editor (.json).
# JSON files are converted to Netscape format automatically.
INSTAGRAM_COOKIES_FILE=/path/to/instagram_cookies.json
